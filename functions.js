const nuggetizer = (animal) => {
    return `processed ${animal}`;
}

console.log(nuggetizer("seitan"));
console.log(nuggetizer("student"));

const addMeaning = (num) => {
    return num + 42;
}

console.log(addMeaning(1));

const ageInYear = (born, year) => {
    return year - born;
}

console.log(ageInYear(1992, 2099));